{
  "name": "notepad",
  "version": "1.0.0",
  "description": "에버노트 스타일 개인 메모장",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "build:server": "npm install --prefix server && npm run prisma:generate --prefix server",
    "render-build": "npm run build:server && npm run build:client && npm run prisma:push --prefix server",
    "start": "npm run start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
