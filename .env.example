# 로컬 개발용 환경 변수 예시 (이 파일을 복사해 server/.env 로 사용하세요)

# Turso(libsql) 연결 정보 - 런타임에서 실제로 사용
TURSO_URL="libsql://your-database-name.turso.io"
TURSO_TOKEN="your-turso-auth-token"

# Prisma CLI(generate/migrate diff) 전용 더미 값 (실제 연결에는 사용되지 않음)
DATABASE_URL="file:./dev.db"

# 서버 포트 (로컬 기본값 5000)
PORT=5000
